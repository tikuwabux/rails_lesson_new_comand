<h3>ユーザー新規作成</h3>

<!--パーシャルの読み込み｡
パーシャル中で参照可能な変数objにインスタンス変数@userを格納-->
<%= render "shared/error", obj: @user %> 


<!-- 上に加え､下にlocal: true を追加することで､validateエラー時のメッセが表示されるようになった!-->

<!-- form_with model: モデルオブジェクト do |f| 
      フォームの中身
     end

でフォームを簡単に作成できる｡ -->

<%= form_with model: @user, local: true do |f| %>
  <table>
    <tr>
      <%# フォームのラベルを作成｡name属性=user[name]｡表記は"名前"とする %>
      <th><%= f.label :name, "名前" %></th>
      <%# テキストボックス(1行のみ入力可)を作成する｡name属性=user[name] %>
      <td><%= f.text_field :name %></td>
    </tr>
    
    <tr>
      <%# フォームのラベルを:emailとする､表記は"メールアドレス"とする%>
      <th><%= f.label :email, "メールアドレス" %></th>
      <td><%= f.text_field :email %></td>
    </tr>
    
    <tr>
      <th><%= f.label :age, "年齢" %></th>
      <td><%= f.text_field :age %></td>
    </tr>
    
    <tr>
      <th><%= f.label :introduction, "一言紹介" %></th>
      <td><%= f.text_area :introduction %></td>
    </tr>
  </table>
  
  <div>
    <ul>
      <li><%= f.submit "登録完了する" %></li>
      <li><%= link_to "ユーザー登録一覧に戻る", :users %></li>
    </ul>
  </div>

<% end %>
